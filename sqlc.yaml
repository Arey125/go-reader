version: "2"
sql:
  - engine: "sqlite"
    queries: "./internal/db/queries"
    schema: "./migrations"
    gen:
      go:
        out: "internal/db/queries"
        overrides:
          - db_type: "TEXT"
            nullable: true
            go_type:
              type: "*string"

